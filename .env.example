# Copy to .env. NEVER commit .env - it is gitignored.
ANTHROPIC_API_KEY=sk-ant-replace-me
CLEARDRAFT_DATA_DIR=data
# Set to 0 to run the pipeline with the model switched off entirely.
# The rule tier must still work. That is the point.
CLEARDRAFT_USE_MODEL=1
